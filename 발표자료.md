# 개발자 포트폴리오 정형화·비교 시스템
### 알고리즘 4조 — 수업 발표 자료

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [레포지토리](#2-레포지토리)
3. [기술 스택](#3-기술-스택)
4. [시스템 아키텍처](#4-시스템-아키텍처)
5. [사용 알고리즘 8종](#5-사용-알고리즘-8종)
   - [#1 Solar LLM — 포트폴리오 파싱·정형화](#알고리즘-1--solar-llm--포트폴리오-파싱정형화)
   - [#2 해시 테이블 — 스펙 매칭](#알고리즘-2--해시-테이블--스펙-매칭)
   - [#3 Edit Distance (DP) — 오타·표기 차이 흡수](#알고리즘-3--edit-distance-dp--오타표기-차이-흡수)
   - [#4 LCS (DP) — 매칭 점수 산출](#알고리즘-4--lcs-dp--매칭-점수-산출)
   - [#5 정렬 (Timsort) — 지원자 순위 정렬](#알고리즘-5--정렬-timsort--지원자-순위-정렬)
   - [#6 BST — 키워드 검색](#알고리즘-6--bst--키워드-검색)
   - [#7 별칭 해시맵 + Edit Distance — 동의어 통합 검색](#알고리즘-7--별칭-해시맵--edit-distance--동의어-통합-검색)
   - [#8 Rabin-Karp + LCS — 유사 문장 검출](#알고리즘-8--rabin-karp--lcs--유사-문장-검출)
6. [알고리즘 간 호출 관계](#6-알고리즘-간-호출-관계)
7. [개발 워크플로우](#7-개발-워크플로우)
8. [주요 기능 시연 흐름](#8-주요-기능-시연-흐름)

---

## 1. 프로젝트 개요

### 해결 문제

면접 서류심사 담당자는 지원자마다 형식이 다른 포트폴리오를 일일이 열어 비교해야 한다.

| 기존 문제 | 해결 방법 |
|-----------|-----------|
| 포트폴리오 형식이 제각각이라 비교가 어렵다 | Solar LLM으로 공통 스키마로 자동 파싱·정형화 |
| 기술명 표기가 사람마다 다르다 (py, 파이선, Python) | Alias 해시맵 + Edit Distance로 동일 기술로 통합 |
| 특정 스펙 보유자를 빠르게 찾기 어렵다 | 해시 테이블 O(1) 매칭 + BST 키워드 검색 |
| 포트폴리오 내용 복사·중복 지원자를 구별하기 어렵다 | Rabin-Karp + LCS 유사 문장 자동 검출 |
| 지원자 간 스킬·경력 점수를 수치화하기 어렵다 | LCS 매칭 점수 + 가중치 정렬 |

### 핵심 기능 요약

- **포트폴리오 업로드** — PDF / MD / TXT 파일 또는 텍스트 붙여넣기 → Solar LLM 자동 파싱
- **스펙 매칭** — 해시 테이블 + LCS로 매칭률 계산, 가중치 기반 정렬
- **키워드 검색** — BST + 별칭 해시맵으로 동의어·오타 포함 통합 검색
- **유사 문장 검출** — Rabin-Karp + LCS로 포트폴리오 간 표절 구간 하이라이트
- **블라인드 심사** — 이름 가리기, 통과/보류/탈락 마킹, 지원자별 메모

---

## 2. 레포지토리

```
https://github.com/cole0213/Algorithm-Group-4-Project
```

### 브랜치 전략

```
main  ←  기능 개발 브랜치 병합
```

### 디렉토리 구조 (핵심)

```
Algorithm-Group-4-Project/
├── Prototype/
│   ├── backend/
│   │   ├── main.py                       # FastAPI 진입점
│   │   ├── routers/
│   │   │   ├── portfolios.py             # 포트폴리오 CRUD·분석·검색·유사문장
│   │   │   └── utils.py                  # 채용 설정 추출, diff 비교
│   │   └── services/
│   │       ├── solar.py                  # Solar LLM 호출·파싱 (MapReduce 포함)
│   │       ├── _solar_http.py            # httpx 비동기 HTTP 래퍼
│   │       ├── parser.py                 # PDF 파싱 (pdfplumber)
│   │       └── algorithms/               # 핵심 알고리즘 8종 (파일별 분리)
│   │           ├── hash_table.py         # #2 해시 테이블
│   │           ├── edit_distance.py      # #3 Edit Distance
│   │           ├── lcs.py                # #4 LCS
│   │           ├── sort.py               # #5 정렬
│   │           ├── bst.py                # #6 BST
│   │           ├── alias_map.py          # #7a 별칭 사전
│   │           ├── alias_search.py       # #7b 별칭 확장 검색
│   │           └── rabin_karp.py         # #8 Rabin-Karp + LCS
│   └── frontend/
│       └── src/
│           ├── pages/                    # 랜딩·메인 화면
│           └── components/              # 사이드바·패널·모달 등 14개 컴포넌트
└── docs/                                 # 기술 문서 (algorithms, features 등)
```

---

## 3. 기술 스택

| 레이어 | 기술 | 역할 |
|--------|------|------|
| **Frontend** | React 18 + Vite 5 | UI, 상태 관리, API 통신 |
| **Markdown 렌더링** | react-markdown + remark-gfm | 자기소개·프로젝트 설명 렌더링 |
| **Backend** | Python 3.10+ + FastAPI + Uvicorn | REST API 서버 |
| **LLM** | Upstage Solar API (`solar-pro`) | 포트폴리오 파싱·요약·설정 추출 |
| **PDF 파싱** | pdfplumber | PDF → 텍스트 추출 |
| **HTTP 클라이언트** | httpx (비동기) / requests (동기) | Solar API 호출 |
| **환경 변수** | python-dotenv | API 키 관리 |
| **데이터 저장** | session.json (서버) + localStorage (브라우저) | 포트폴리오 세션 유지 |
| **스타일링** | 순수 CSS | Pretendard·JetBrains Mono 폰트 |

---

## 4. 시스템 아키텍처

```
브라우저 (React + Vite, :5173)
        │  /api/* → Vite proxy
        ▼
FastAPI 서버 (:8000)
   │
   ├── routers/portfolios.py
   │     ├── POST /portfolios/add         → parser.py → solar.py → session.json
   │     ├── POST /analyze                → hash_table + lcs + sort
   │     ├── GET  /search                 → alias_search + bst
   │     └── GET  /similar                → rabin_karp + lcs
   │
   ├── routers/utils.py
   │     ├── POST /extract-specs          → solar.py (채용 공고 파싱)
   │     ├── POST /extract-config         → solar.py (설정 자동 생성)
   │     └── POST /diff                   → solar.py (N명 비교)
   │
   └── services/
         ├── solar.py      ← Upstage Solar API (MapReduce 청킹)
         ├── parser.py     ← pdfplumber PDF 추출
         └── algorithms/   ← 알고리즘 8종
```

### 포트폴리오 데이터 스키마 (Solar 파싱 결과)

```json
{
  "id":          "홍길동",
  "name":        "홍길동",
  "email":       "example@email.com",
  "github":      "https://github.com/example",
  "career_years": 3,
  "education":   "OO대학교 컴퓨터공학과",
  "skills":      ["React", "Python", "Docker", "AWS"],
  "intro":       "자기소개 원문 텍스트...",
  "projects": [
    { "name": "프로젝트명", "period": "2023.01-2023.06",
      "role": "백엔드 개발", "stack": "Python, FastAPI", "desc": "설명..." }
  ],
  "awards":  ["정보처리기사"],
  "links":   [{ "label": "GitHub", "url": "https://github.com/..." }]
}
```

---

## 5. 사용 알고리즘 8종

> 경로 공통 prefix: `Prototype/backend/services/`

---

### 알고리즘 #1 · Solar LLM · 포트폴리오 파싱·정형화

**역할**: PDF·텍스트 원문을 Solar API에 전달해 공통 JSON 스키마로 변환한다.

**파이프라인**:

```
PDF 파일 → pdfplumber(parser.py) → 텍스트 추출
                                        ↓
          32k 토큰 이내? ─── YES ──→ Solar 단일 호출
                │                        ↓
               NO                   JSON 파싱 + 정규화
                ↓
          섹션 헤더 기준 청크 분할 (MapReduce Map 단계)
                ↓
          청크별 Solar 호출 (병렬 의미로 순차 처리)
                ↓
          Solar Reduce 호출 → 청크 결과 통합
                ↓
          최종 포트폴리오 JSON
```

**Solar 컨텍스트 한계 대응 (MapReduce)**:

| 단계 | 동작 |
|------|------|
| 분할 (Map) | 마크다운 헤더·섹션 키워드 경계 기준으로 30,000자 이내 청크로 분리 |
| 개별 파싱 | 청크별 독립 Solar 호출 → 각각 JSON 반환 |
| 통합 (Reduce) | Solar LLM이 여러 청크 결과를 하나의 완전한 JSON으로 합성 |

**후처리 — skills 필터링**:
- 블랙리스트 60+항목 제거 (직무명·소프트스킬·패러다임 등)
- 별칭 사전 기반 정규화 중복 제거 (JavaScript + JS → JavaScript 1개)
- 최대 25개 상한

**구현 위치**: `services/solar.py`

---

### 알고리즘 #2 · 해시 테이블 · 스펙 매칭

**역할**: 채용 담당자가 입력한 필요 스펙을 해시셋에 저장하고 지원자 스킬과 O(1)로 매칭한다.

**구현**: Python `set` (내부적으로 해시 테이블, 버킷 체이닝)

**시간복잡도**: 삽입 O(1) 평균 / 조회 `__contains__` O(1) 평균

**동작 예시**:

```
필요 스펙:  ["React", "Python", "Docker"]
해시셋:     {"react", "python", "docker"}  ← 소문자 정규화 후 저장

지원자 스킬:  ["React", "Vue", "Python", "Git"]
매칭 결과:
  "React"  → "react" ∈ 해시셋 → True  (O(1))
  "Vue"    → "vue"   ∉ 해시셋 → False (O(1))
  "Python" → "python" ∈ 해시셋 → True  (O(1))
  "Git"    → "git"   ∉ 해시셋 → False (O(1))
```

**핵심 클래스**:

```python
class SpecMatcher:
    def __init__(self, required_specs: list[str]):
        self._required: set[str] = set()       # 해시 테이블
        for spec in required_specs:
            self._required.add(spec.strip().lower())

    def is_required(self, skill: str) -> bool:
        return skill.strip().lower() in self._required   # O(1)

    def match_skills(self, skills: list[str]) -> dict[str, bool]:
        return {s: self.is_required(s) for s in skills}  # O(n)
```

**구현 위치**: `services/algorithms/hash_table.py`

---

### 알고리즘 #3 · Edit Distance (DP) · 오타·표기 차이 흡수

**역할**: `파이선`, `파이썬`, `Python`처럼 표기가 다른 기술명을 같은 기술로 인식한다.

**점화식 (Wagner-Fischer)**:

```
dp[i][j] =
  dp[i-1][j-1]                              (a[i-1] == b[j-1], 동일)
  1 + min(dp[i-1][j],   # 삭제
          dp[i][j-1],   # 삽입
          dp[i-1][j-1]) # 대체              (a[i-1] != b[j-1])
```

**시간복잡도**: O(|a| × |b|) / **공간복잡도**: O(|a| × |b|)

**동작 예시**:

```
edit_distance("Python", "파이썬") = 6   → 임계값(2) 초과 → 불일치
edit_distance("Python", "Pytohn") = 2   → 임계값(2) 이하 → 유사 인정
edit_distance("React",  "ReactJS") = 2  → 임계값(2) 이하 → 유사 인정
```

**사용처**:

| 기능 | 역할 |
|------|------|
| 별칭 검색 (#7) | 검색어와 전체 별칭 목록 비교 — 오타 흡수 |
| 스펙 매칭 (#2) | 기술명 표기 차이 보완 (alias_search 경유) |

**구현 위치**: `services/algorithms/edit_distance.py`

---

### 알고리즘 #4 · LCS (DP) · 매칭 점수 산출

**역할**: 필요 스펙 목록과 지원자 스킬 목록의 최장 공통 부분 수열 길이로 0~100% 매칭 점수를 산출한다.

**점화식 (롤링 배열 최적화)**:

```
curr[j] = prev[j-1] + 1              (a[i-1] == b[j-1])
curr[j] = max(prev[j], curr[j-1])    (a[i-1] != b[j-1])
```

**시간복잡도**: O(m × n) / **공간복잡도**: O(n) (롤링 배열)

**매칭 점수 공식**:

```
match_score = LCS(필요스펙, 지원자스킬) / |필요스펙| × 100 (%)
```

**동작 예시**:

```
필요 스펙:   [React, Python, Docker, AWS, Git]   (5개)
지원자 스킬: [React, Vue, Python, Git, Linux]

LCS:  React, Python, Git  →  길이 3
매칭 점수: 3 / 5 × 100 = 60%
```

**사용처**:

| 기능 | 역할 |
|------|------|
| 스펙 매칭 | 0~100% 매칭 점수 산출 |
| 유사 문장 검출 (#8) | Rabin-Karp 해시 충돌 구간의 LCS 유사도 검증 (70% 이상만 통과) |

**구현 위치**: `services/algorithms/lcs.py`

---

### 알고리즘 #5 · 정렬 (Timsort) · 지원자 순위 정렬

**역할**: 매칭률·경력·이름 기준으로 지원자 목록을 정렬한다.

**구현**: Python 내장 `sorted()` — Timsort 기반 (합병 정렬 + 삽입 정렬 하이브리드)

**시간복잡도**: O(n log n) 평균·최악 / **공간복잡도**: O(n)

**가중치 종합 점수 공식**:

```
weighted_score = skill_score × w_skill
               + career_score × w_career
               + project_score × w_project

career_score  = min(career_years / 10, 1.0) × 100
project_score = min(project_count / 10, 1.0) × 100
기본 가중치:  skill 60% / career 25% / project 15%
```

**정렬 기준 3가지**:

| 기준 | 설명 |
|------|------|
| `match` | weighted_score 내림차순 (기본) |
| `career` | career_years 내림차순 |
| `name` | 이름 오름차순 |

**구현 위치**: `services/algorithms/sort.py`

---

### 알고리즘 #6 · BST · 키워드 검색

**역할**: 키워드로 포트폴리오를 검색한다. **전체(cross) 검색**과 **패널 내(intra) 검색** 두 모드를 BST로 구현한다.

**시간복잡도**: 삽입·검색 O(log n) 평균, O(n) 최악 (비균형 트리)

**두 가지 인덱스 클래스**:

| 클래스 | 모드 | 인덱싱 대상 | 반환 |
|--------|------|-------------|------|
| `ApplicantIndex` | cross | 스킬·이름·경력 키워드 → 지원자 ID | 매칭 지원자 ID 목록 |
| `TextIndex` | intra | 단일 포트폴리오 내 단어 → 위치 | ±5단어 스니펫 + 문자 위치 |

**BST 노드 구조**:

```python
@dataclass
class _Node:
    key: str                  # 키워드 (소문자)
    value: list               # ID 목록 또는 위치 목록
    left:  _Node | None
    right: _Node | None
```

**캐시 전략**: 인덱스를 메모리에 보관하고, 포트폴리오 추가·삭제·재분석·임포트 시 `invalidate_cache()`로 무효화 후 다음 검색 시 재빌드

**구현 위치**: `services/algorithms/bst.py`

---

### 알고리즘 #7 · 별칭 해시맵 + Edit Distance · 동의어 통합 검색

**역할**: `py`, `파이선`, `파이썬`을 입력해도 `Python` 관련 결과를 모두 반환한다.

**시간복잡도**: O(k × m) — k = 총 별칭 수, m = 검색어 길이

**구조 — 2단계 파이프라인**:

```
사용자 입력: "py"
      │
      ▼
[#7a 별칭 해시맵] alias_map.py
  ALIAS_MAP = {
    "python": ["py", "파이썬", "파이선", "Python", "python3", ...]
    "react":  ["리액트", "ReactJS", "react.js", ...]
    ...         (150+ 항목)
  }
  REVERSE_MAP = { "py": "python", "파이썬": "python", ... }
  → O(1) 조회: "py" → 정규키 "python" → 전체 별칭 목록 획득

      │
      ▼
[#7b Edit Distance 퍼지 매칭] alias_search.py
  → 쿼리와 전체 별칭 목록을 Edit Distance 비교
  → 임계값(2) 이하인 별칭도 확장 쿼리에 추가 (오타 흡수)
  → 최종 확장 쿼리: ["python", "py", "파이썬", "파이선", "Python", ...]

      │
      ▼
포트폴리오 스킬 목록에서 확장 쿼리 중 하나라도 포함하면 매칭
```

**별칭 사전 규모**: 언어·프레임워크·DB·클라우드·인프라 150+ 항목

**구현 위치**: `services/algorithms/alias_map.py`, `alias_search.py`

---

### 알고리즘 #8 · Rabin-Karp + LCS · 유사 문장 검출

**역할**: 두 포트폴리오 사이에서 유사하게 작성된 문장 구간을 찾아 색상 그룹으로 표시한다.

**시간복잡도**: O(n + m) 평균 (해싱), O(n × m) 최악 (LCS 검증)

**전체 파이프라인**:

```
포트폴리오 A, B 텍스트
        ↓
[1] 토크나이즈 (_tokenize)
    → (소문자_정제어, 원문_시작_char, 원문_끝_char) 목록

        ↓
[2] 슬라이딩 윈도우 롤링 해시 (_rolling_hashes)  ← Rabin-Karp
    → { 해시값: [시작_인덱스, ...] }

        ↓
[3] 두 포트폴리오 해시 충돌 구간 수집
    → matched_a: {pos_a}, matched_b: {pos_b}

        ↓
[4] 충돌 구간 LCS 유사도 검증 (≥ 70%만 통과)  ← LCS (#4 재활용)

        ↓
[5] 인접 구간 병합 (merge_ranges)
    → [(start, end), ...]

        ↓
[6] 섹션 경계 분할 (_section_spans)  ← 오검출 방지
    → 원문 위치에서 span 텍스트 추출

        ↓
[7] 독립 구간별 색상 그룹 번호 부여 → SimilarSpan 목록
    (한 쌍에서 구간이 여러 개면 각각 다른 그룹 번호 할당)
```

**롤링 해시 공식**:

```
초기:    H = (w₁×31⁴ + w₂×31³ + w₃×31² + w₄×31 + w₅) mod P
슬라이딩: H = (H − w_prev × 31^(window-1) + w_new) mod P
P = 2³¹ − 1 (메르센 소수), BASE = 31, WINDOW = 5단어
```

**LCS 검증 (false positive 제거)**:

```python
lcs = lcs_length(chunk_a, chunk_b)   # chunk: 5단어짜리 윈도우
if lcs / WORD_WINDOW >= 0.7:          # 70% 이상 → 유사 구간 확정
    matched_a.add(pos_a)
    matched_b.add(pos_b)
```

**구현 위치**: `services/algorithms/rabin_karp.py`

---

## 6. 알고리즘 간 호출 관계

```
routers/portfolios.py
  │
  ├─ POST /analyze
  │     ├─ SpecMatcher (hash_table)     → O(1) 스킬 매칭
  │     ├─ match_score (lcs)            → LCS 매칭 점수
  │     └─ sort_applicants (sort)       → Timsort 정렬
  │
  ├─ GET /search
  │     ├─ portfolio_matches_query      → expand_query (alias_search)
  │     │     └─ edit_distance          → 오타 흡수
  │     ├─ get_or_build_bst             → ApplicantIndex.search   (cross BST)
  │     └─ TextIndex + search_context  → 위치·스니펫 반환         (intra BST)
  │
  └─ GET /similar
        └─ detect_similar_response (rabin_karp)
              ├─ _rolling_hashes        → Rabin-Karp 해시
              └─ lcs_length (lcs)       → LCS 검증
```

**알고리즘 복잡도 요약**:

| # | 알고리즘 | 시간복잡도 | 공간복잡도 |
|---|----------|-----------|-----------|
| 1 | Solar LLM | API 의존 | — |
| 2 | 해시 테이블 | O(1) 평균 | O(n) |
| 3 | Edit Distance | O(m × n) | O(m × n) |
| 4 | LCS | O(m × n) | O(n) 롤링 배열 |
| 5 | Timsort | O(n log n) | O(n) |
| 6 | BST | O(log n) 평균 | O(n) |
| 7 | 별칭 해시맵 + Edit Distance | O(k × m) | O(k) |
| 8 | Rabin-Karp + LCS | O(n+m) 평균 | O(n) |

---

## 7. 개발 워크플로우

### 개발 사이클

```
수정필요사항.md 작성 (요구사항·버그 목록)
        ↓
구현 (코드 변경)
        ↓
수정사항.md 작성 (반영 결과)
        ↓
git commit → push → PR 병합
```

### 스프린트별 진행 내역

| Phase | 기간 | 주요 내용 |
|-------|------|-----------|
| **Phase 0** | 5월 8일 | React + FastAPI 골격, Solar API 연동, PDF → JSON 파이프라인, 알고리즘 8종 초기 구현 |
| **Phase 1** | 5월 20일 | Prototype v2 — 알고리즘 8종 통합, 사이드바 + 4패널 구조 확립, 유사 문장·스킬 매칭 기본 동작 |
| **Phase 2** | 5월 24일 | 폴더 일괄 업로드, 다크 모드, AI 설정 자동 추출, 유사 문장 팔레트 테마 방식 전환 |
| **Phase 3** | 5월 25일 | react-markdown 도입, 타임라인(Timeline.jsx) 신규, JobConfigModal 통합, 기술 스택 추출 정확도 개선 |
| **Phase 4** | 5월 28일 | `docs/` 폴더 신설 — 기술 문서화 전면 정비 |
| **Phase 5** | 5월 28일 | 미사용 import 제거, 구 문서 삭제 및 `docs/`로 통합, workflow·backlog·deploy 문서 신설 |
| **Phase 6** | 5월 30일 | Solar 재시도 백오프(`_call_solar_api`), Rabin-Karp 그룹 번호 개선, 포트폴리오 요약 API 추가 |

### 주요 버그 수정 이력

| 버그 | 원인 | 수정 방법 |
|------|------|-----------|
| Windows cp949 em 대시 UnicodeEncodeError → 500 오류 | 인코딩 처리 누락 | `solar.py`, `portfolios.py`에 UTF-8 강제 인코딩 |
| 유사 문장 그룹 6 이상에서 색상 `undefined` | 색상 배열 인덱스 오버플로 | `api.js` 모듈러 연산 적용 |
| 유사 문장 윈도우 병합 누락·섹션 경계 초과 | `_tokenize`, `_merge_ranges` 미구현 | `rabin_karp.py` `_section_spans` 추가로 근본 해결 |
| httpx 미설치로 diff 엔드포인트 JSON 오류 | `requirements.txt` 누락 | `httpx` 의존성 추가 |

---

## 8. 주요 기능 시연 흐름

### 포트폴리오 업로드 → 파싱

```
[사용자] PDF 파일 선택 (UploadModal)
  → api.js: addPortfolio()
  → POST /api/portfolios/add
  → parser.py: extract_pdf_text()  (pdfplumber)
  → solar.py: parse_text()          (Solar LLM)
  → session.json 저장
  → 사이드바에 지원자 추가 + 패널 자동 열기
```

### 스펙 매칭 → 점수 계산 → 정렬

```
[사용자] 채용 공고 붙여넣기 (JobConfigModal)
  → POST /api/extract-config        (Solar LLM 자동 추출)
  → 필요 스펙·가중치·필터 자동 설정
  → [Apply] 클릭
  → POST /api/analyze
      → hash_table: SpecMatcher.match_skills()   O(1) 매칭
      → lcs: match_score()                       LCS 0–100%
      → 가중치 종합 점수 계산
      → sort: sort_applicants()                  Timsort
  → 사이드바: 매칭률 배지(#N%, 색상) 표시
  → 패널: 일치 스킬 노란 하이라이트
```

### 키워드 검색

```
[사용자] "py" 입력 (TopBar 검색창, 300ms 디바운스)
  → GET /api/search?q=py&mode=cross
      → alias_search: expand_query("py")         별칭 확장
            → alias_map: "py" → "python" 역방향 조회  O(1)
            → edit_distance: 오타 후보 추가
        → 확장 쿼리: ["python", "Python", "py", "파이썬", ...]
      → portfolio_matches_query()                 스킬 대조
      → bst: ApplicantIndex.search("py")          BST 탐색  O(log n)
  → 사이드바: Python 보유 지원자만 필터링 표시
```

### 유사 문장 검출

```
[자동] 패널 2개 이상 열리면 트리거
  → GET /api/similar?ids=A,B
      → rabin_karp: detect_similar([A, B])
            → _tokenize()                         토큰 위치 추적
            → _rolling_hashes()                   Rabin-Karp 슬라이딩 해시
            → 해시 충돌 구간 수집
            → lcs_length()                        LCS 70% 검증
            → merge_ranges()                      인접 구간 병합
            → _section_spans()                    섹션 경계 분할
            → 그룹 번호 부여
  → 패널: 유사 구간 색상 그룹으로 하이라이트
```

---

*발표자료 최종 업데이트: 2026-05-30*
*레포지토리: https://github.com/cole0213/Algorithm-Group-4-Project*

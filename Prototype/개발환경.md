# 개발 환경 설정

## 기술 스택

| 영역 | 선택 | 비고 |
| ---- | ---- | ---- |
| Frontend | React + Vite | |
| Backend | Python + FastAPI | |
| PDF 파싱 | pdfplumber | |
| LLM | Upstage Solar API | |
| 패키지 관리 (FE) | npm | |
| 패키지 관리 (BE) | pip + requirements.txt | |
| 실행 방식 | 로컬 단독 실행 | |

---

## 디렉토리 구조

```
project/
├── frontend/          # React + Vite
│   ├── src/
│   │   ├── components/
│   │   └── main.jsx
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
├── backend/           # Python + FastAPI
│   ├── routers/
│   ├── services/
│   │   ├── solar.py       # Solar API 연동
│   │   ├── parser.py      # PDF / 텍스트 파싱
│   │   └── algorithms/    # 알고리즘 구현
│   │       ├── hash_table.py
│   │       ├── edit_distance.py
│   │       ├── lcs.py
│   │       ├── sort.py
│   │       ├── bst.py
│   │       ├── alias_map.py
│   │       ├── alias_search.py
│   │       └── rabin_karp.py
│   ├── main.py
│   ├── session.json       # 자동 생성 — .gitignore 처리됨
│   └── requirements.txt
```

---

## Frontend 설치

### 요구사항
- Node.js 18 이상

### 초기 설정
```bash
cd frontend
npm install
```

### 개발 서버 실행
```bash
npm run dev
# http://localhost:5173
```

---

## Backend 설치

### 요구사항
- Python 3.10 이상

### 가상환경 생성 및 활성화
```bash
cd backend
python -m venv venv

# Windows
venv\Scripts\activate

# macOS / Linux
source venv/bin/activate
```

### 패키지 설치
```bash
pip install -r requirements.txt
```

### requirements.txt
```
fastapi
uvicorn[standard]
python-multipart
pdfplumber
requests
python-dotenv
```

### 개발 서버 실행
```bash
uvicorn main:app --reload
# http://localhost:8000
```

---

## 환경 변수

`backend/.env` 파일 생성:

```
SOLAR_API_KEY=your_api_key_here
SOLAR_MODEL=solar-pro
```

- `SOLAR_API_KEY`: Upstage 콘솔에서 발급
- `SOLAR_MODEL`: Solar 모델명 (기본값 `solar-pro`, 변경 시 이 값만 수정)

---

## Frontend ↔ Backend 연결

- Frontend에서 Backend로 요청 시 `http://localhost:8000` 으로 통신
- Vite 개발 서버 프록시 설정 (`vite.config.js`):

```js
export default {
  server: {
    proxy: {
      '/api': 'http://localhost:8000'
    }
  }
}
```

---

## 실행 순서

```
1. backend 가상환경 활성화
2. uvicorn main:app --reload  (터미널 1)
3. cd frontend && npm run dev  (터미널 2)
4. 브라우저에서 http://localhost:5173 접속
```

# 검증 리스트 — Portfolio Reviewer

> 각 항목은 Playwright 또는 수동으로 검증 후 `[x]`로 체크.
> 버그 발견 시 `[BUG]` 태그 추가 후 재-계획-작업-검증 사이클 진행.

---

## 체크리스트 범례

| 기호 | 의미 |
|------|------|
| `[ ]` | 미검증 |
| `[x]` | 검증 완료 |
| `[BUG]` | 버그 발견 — 재작업 필요 |
| `[SKIP]` | 검증 불필요 (구조적으로 보장됨) |

---

## A. 유사 문장 검출 (이번 세션 변경)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| A-1 | 패널 1개만 열렸을 때 유사 문장 하이라이트가 **없어야** 함 | 패널 1개 열기 → 하이라이트 없음 확인 | `[ ]` |
| A-2 | 패널 2개 열렸을 때 유사 문장 하이라이트가 **표시**됨 | 같은 스킬 보유자 2명 패널 열기 → 하이라이트 확인 | `[ ]` |
| A-3 | 패널 닫아 1개 남기면 하이라이트 **사라짐** | 2개 → 1개로 패널 닫기 → 하이라이트 사라짐 확인 | `[ ]` |
| A-4 | 같은 포트폴리오(자기 자신)와 유사 문장 비교 안 됨 | 백엔드 `/similar?ids=단일ID` 호출 → spans 빈 배열 | `[ ]` |

---

## B. Solar AI 재분석 버튼

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| B-1 | 패널 상단에 "Solar AI 재분석" 버튼 표시 | 패널 열기 → 버튼 존재 확인 | `[x]` (`reanalyzeBtnText: "Solar AI 재분석"` 확인됨) |
| B-2 | "원본 보기 →" 버튼과 나란히 표시 | 스크린샷으로 panel-top-actions 확인 | `[x]` (스크린샷 확인됨) |
| B-3 | 클릭 시 confirm 대화상자 표시 | 버튼 클릭 → "Solar AI로 재분석하시겠습니까?" 확인 | `[ ]` |
| B-4 | Solar API 미설정 시 503 에러 메시지 toast 표시 | `.env` 없는 환경에서 클릭 → 실패 toast 표시 | `[ ]` |
| B-5 | 재분석 성공 시 패널 내용 실시간 업데이트 | 재분석 후 이름·스킬 등 변경 여부 확인 | `[ ]` |
| B-6 | 재분석 후 match_score가 기존 값 유지 | 재분석 전후 뱃지 % 비교 | `[ ]` |
| B-7 | 버튼 disabled 상태 → "분석 중..." 텍스트 | 느린 네트워크 환경 또는 Network Throttle에서 확인 | `[ ]` |

---

## C. 0% 매칭 회색 처리

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| C-1 | 0% 뱃지 클래스가 `match-none` (회색) | `.match-none` 요소 개수 확인 | `[x]` (3개 확인됨) |
| C-2 | 1~39% 뱃지는 여전히 `match-low` (빨강) | 33% 뱃지 클래스 확인 | `[ ]` |
| C-3 | 40~69%는 `match-mid` (주황), 70%+는 `match-high` (초록) | 67% 뱃지 클래스 확인 | `[ ]` |
| C-4 | 패널 헤더 뱃지도 동일하게 회색 처리 | 0% 지원자 패널 열기 → 헤더 뱃지 색상 확인 | `[ ]` |

---

## D. 이름 잘림 툴팁

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| D-1 | 이름 span에 `title` 속성 존재 | `document.querySelector('.applicant-name').title` 확인 | `[ ]` |
| D-2 | title에 실제 이름 + 더블클릭 안내 포함 | title 값: `"이름 · 더블클릭으로 이름 편집"` 형식 확인 | `[ ]` |
| D-3 | 잘린 이름 hover 시 전체 이름 tooltip 표시 | 브라우저 hover 동작 수동 확인 | `[ ]` |

---

## E. 빈 상태 추가 버튼

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| E-1 | 포트폴리오 0건일 때 Sidebar에 빈 상태 + 추가 버튼 표시 | 모든 포트폴리오 삭제 후 Sidebar 확인 | `[ ]` |
| E-2 | 빈 상태 추가 버튼 클릭 시 업로드 모달 열림 | 버튼 클릭 → `.upload-modal` 존재 확인 | `[ ]` |
| E-3 | 포트폴리오 0건일 때 PortfolioArea에 "포트폴리오가 없습니다" + 추가 버튼 표시 | 동일 조건에서 우측 영역 확인 | `[ ]` |
| E-4 | 포트폴리오 있지만 패널 미선택 시 "좌측 목록에서 클릭" 메시지 유지 | 기존 상태 확인 | `[x]` (스크린샷에서 확인됨) |

---

## F. 토스트 알림

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| F-1 | `.toaster` 컴포넌트가 DOM에 마운트됨 (토스트 없을 때 null 반환) | `document.querySelector('.toaster')` → null 정상 | `[x]` |
| F-2 | 포트폴리오 추가 시 "XXX 추가 완료" 초록 toast 표시 | 파일 업로드 완료 후 우측 하단 확인 | `[ ]` |
| F-3 | 포트폴리오 삭제 시 "XXX 삭제 완료" toast 표시 | 삭제 confirm 후 toast 확인 | `[ ]` |
| F-4 | 불러오기 완료 시 toast 표시 (alert 제거됨) | JSON import 후 alert 없이 toast 확인 | `[ ]` |
| F-5 | toast 3초 후 자동 사라짐 | 토스트 표시 후 3초 대기 → 사라짐 확인 | `[ ]` |
| F-6 | 에러 toast는 빨간색 배경 | 실패 케이스에서 `.toast-error` 클래스 확인 | `[ ]` |
| F-7 | 여러 toast 동시 표시 가능 (stack) | 빠르게 연속 액션 수행 → 다중 toast 확인 | `[ ]` |

---

## G. Esc 키보드 단축키

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| G-1 | 업로드 모달 열린 상태에서 Esc → 모달 닫힘 | 모달 열기 → Esc → `.upload-modal` 없어짐 확인 | `[ ]` |
| G-2 | 설정 드로어 열린 상태에서 Esc → 드로어 닫힘 | 드로어 열기 → Esc → `.settings-drawer` 없어짐 확인 | `[ ]` |
| G-3 | 패널 1개 열린 상태에서 Esc → 패널 닫힘 | 패널 열기 → Esc → `.portfolio-panel` 개수 0 확인 | `[BUG]` Playwright 환경에서 패널 미닫힘 확인됨 — 재검증 필요 |
| G-4 | 패널 2개 열린 상태에서 Esc → 마지막 패널만 닫힘 | 패널 2개 → Esc → 1개만 남음 확인 | `[ ]` |
| G-5 | 모달 없고 패널 없는 상태에서 Esc → 아무 변화 없음 | 빈 상태에서 Esc → 에러 없음 확인 | `[ ]` |

> **G-3 BUG 재검증 방법:** `window.addEventListener('keydown', ...)` 리스너가 body focus 상태에서 이벤트를 수신하는지 확인. `document.body.click()` 후 Esc 재시도.

---

## H. 스킬 태그 접기/펼치기

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| H-1 | 스킬 5개 이하 → 더보기 버튼 미표시 | 스킬 ≤5명 패널 열기 → `.skill-toggle-btn` 없음 확인 | `[ ]` |
| H-2 | 스킬 6개 이상 → "+N개 더보기" 버튼 표시 | 스킬 많은 지원자 패널 열기 → 버튼 텍스트 "+X개 더보기" 확인 | `[ ]` |
| H-3 | 더보기 클릭 → 전체 스킬 표시, 버튼 "접기"로 변경 | 클릭 후 모든 스킬 태그 노출 확인 | `[ ]` |
| H-4 | 접기 클릭 → 다시 5개만 표시 | 접기 클릭 후 뱃지 5개로 줄어듦 확인 | `[ ]` |
| H-5 | 매칭된 스킬이 5개 밖에 있어도 하이라이트 표시 | 6번째 이상 스킬 매칭 후 더보기 → 하이라이트 확인 | `[ ]` |

---

## I. 통과/탈락/보류 마킹

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| I-1 | 지원자 항목에 마킹 버튼(·) 표시 | `.mark-btn` 요소 존재 확인 | `[ ]` |
| I-2 | 클릭 1회 → 통과(✓, 초록) | 버튼 클릭 → `.mark-pass` 클래스, 초록 배경 확인 | `[ ]` |
| I-3 | 클릭 2회 → 보류(?, 주황) | 2회 클릭 → `.mark-hold` 클래스 확인 | `[ ]` |
| I-4 | 클릭 3회 → 탈락(✗, 빨강) | 3회 클릭 → `.mark-fail` 클래스, 항목 희미해짐(0.65) 확인 | `[ ]` |
| I-5 | 클릭 4회 → 마킹 초기화 | 4회 클릭 → 버튼 `·` 복귀, 클래스 없음 확인 | `[ ]` |
| I-6 | 마킹이 localStorage에 저장됨 | 마킹 후 페이지 새로고침 → 마킹 유지 확인 | `[ ]` |
| I-7 | 탈락 항목 좌측에 빨간 테두리 표시 | `.marked-fail` border-left 스타일 확인 | `[ ]` |
| I-8 | 마킹 버튼 클릭이 패널 토글에 영향 안 줌 (`stopPropagation`) | 마킹 버튼 클릭 후 패널 열림/닫힘 없음 확인 | `[ ]` |

---

## J. Solar 뱃지 표기 명확화

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| J-1 | Solar LLM 파싱된 패널 헤더에 `Solar` 뱃지 표시 | `_solar_used=true` 포트폴리오 패널 열기 → 탭에 "Solar" 텍스트 확인 | `[ ]` |
| J-2 | `Solar` 뱃지 hover 시 tooltip 표시 | "Solar LLM으로 파싱된 포트폴리오" 문구 확인 | `[ ]` |
| J-3 | 이전 "AI" 텍스트가 더 이상 표시되지 않음 | 패널 탭 내 "AI" 텍스트 없음 확인 | `[ ]` |
| J-4 | `_solar_used=false` 포트폴리오에는 뱃지 미표시 | 비Solar 파싱 포트폴리오 패널 → 뱃지 없음 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 패널 탭 Solar 뱃지 확인
> document.querySelector('.panel-ai-label')?.textContent  // → "Solar"
> document.querySelector('.panel-ai-label')?.title        // → "Solar LLM으로 파싱된..." 문구
> ```

---

## K. 패널 섹션 아코디언

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| K-1 | 패널에 4개 이상의 `.section-header` 요소 존재 | `document.querySelectorAll('.section-header').length` ≥ 4 확인 | `[ ]` |
| K-2 | 섹션 헤더에 ▾ (펼침) 아이콘 표시 | `.section-toggle` 텍스트 `▾` 확인 | `[ ]` |
| K-3 | 헤더 클릭 → 해당 섹션 내용 숨김, ▾ → ▸ 아이콘 변경 | "기본 정보" 헤더 클릭 → ul 사라짐 + 아이콘 ▸ 확인 | `[ ]` |
| K-4 | 헤더 재클릭 → 내용 다시 표시, ▸ → ▾ 복귀 | 접힌 상태에서 재클릭 → 내용 복귀 확인 | `[ ]` |
| K-5 | 섹션 독립 제어 — 일부만 접어도 다른 섹션 영향 없음 | "기술 스택" 접기 → "자기소개" 그대로 표시 확인 | `[ ]` |
| K-6 | 기본 상태는 모두 펼쳐진 상태 | 패널 최초 열기 → 모든 섹션 내용 표시 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 섹션 헤더 수
> document.querySelectorAll('.section-header').length  // ≥ 4
> // 접기 후 아이콘
> document.querySelector('.section-toggle').textContent  // '▾' 또는 '▸'
> ```
>
> **수동 절차:**
> 1. 패널 열기
> 2. "기본 정보" 섹션 헤더 클릭 → 내용 숨김 + ▸ 아이콘 확인
> 3. 재클릭 → 내용 복귀 + ▾ 아이콘 확인
> 4. "기술 스택" 접기 → "자기소개" 영향 없음 확인

---

## L. 분석 결과 요약 배너

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| L-1 | 분석 버튼 클릭 후 `.analyze-summary-banner` 표시 | "분석" 클릭 → 배너 DOM 존재 확인 | `[ ]` |
| L-2 | 배너에 "총 N명 중 M명이 70% 이상 매칭" 텍스트 표시 | 배너 텍스트 내용 확인 (N·M 숫자 실제 데이터 반영) | `[ ]` |
| L-3 | 배너 우측 ✕ 버튼 클릭 → 배너 사라짐 | `.banner-close` 클릭 → 배너 없어짐 확인 | `[ ]` |
| L-4 | 배너 배경색이 초록 계열 (70%+ 강조) | `.analyze-summary-banner` 배경색 #DCFCE7 확인 | `[ ]` |
| L-5 | 페이지 첫 로드 시에도 배너 표시 (초기 runAnalyze 호출) | 새로고침 후 배너 자동 표시 확인 | `[ ]` |
| L-6 | 스펙 변경 후 재분석 시 배너 수치 업데이트 | 다른 스펙 입력 → 분석 → 배너 수치 변경 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 배너 존재 확인
> document.querySelector('.analyze-summary-banner')?.textContent
> // 예: "📊 분석 완료 — 총 11명 중 3명이 70% 이상 매칭"
> ```
>
> **수동 절차:**
> 1. 페이지 로드 → TopBar 아래에 배너 자동 표시 확인
> 2. 배너 텍스트에서 총 인원 = 사이드바 목록 수와 일치 확인
> 3. ✕ 클릭 → 배너 사라짐 확인
> 4. "분석" 버튼 재클릭 → 배너 다시 표시 확인

---

## M. 지원자 메모/코멘트

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| M-1 | 패널 하단에 `.panel-note-section` 영역 존재 | `document.querySelector('.panel-note-textarea')` 존재 확인 | `[ ]` |
| M-2 | textarea 입력 시 실시간 반영 | 메모 입력 → 텍스트 표시 확인 | `[ ]` |
| M-3 | 메모가 localStorage에 저장됨 (`portfolio-reviewer-notes`) | 메모 입력 → `localStorage.getItem('portfolio-reviewer-notes')` 확인 | `[ ]` |
| M-4 | 페이지 새로고침 후 메모 유지 | F5 새로고침 → 동일 지원자 패널 열기 → 메모 내용 그대로 확인 | `[ ]` |
| M-5 | 다른 지원자 패널에는 해당 지원자 메모가 표시되지 않음 | 지원자 A 메모 작성 → 지원자 B 패널 열기 → 메모 비어있음 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 메모 저장 확인
> const notes = JSON.parse(localStorage.getItem('portfolio-reviewer-notes'));
> console.log(notes);  // { [id]: "메모 내용" }
> ```

---

## N. 사이드바 순위 뱃지 (#N 형식)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| N-1 | 지원자 이름 앞에 `.applicant-rank` 뱃지 표시 | `document.querySelector('.applicant-rank')?.textContent` → `"#1"` 확인 | `[ ]` |
| N-2 | 뱃지 형식이 `#숫자` (예: `#1`, `#2`) | 목록 첫 항목 뱃지 텍스트 확인 | `[ ]` |
| N-3 | 정렬 변경 시 번호 재정렬 | 정렬 기준 변경 → 순번 재배치 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 모든 순위 뱃지 텍스트 확인
> [...document.querySelectorAll('.applicant-rank')].map(el => el.textContent)
> // → ["#1", "#2", "#3", ...]
> ```

---

## O. 검색 필터 패널 경고

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| O-1 | 검색어 입력으로 필터된 상태에서 기존 열린 패널에 `.panel-filter-warn` 표시 | 패널 열기 → 검색어로 해당 지원자 필터 제외 → 패널 하단 경고 확인 | `[ ]` |
| O-2 | 경고 메시지: "현재 검색 결과에 없는 지원자입니다" | `.panel-filter-warn` 텍스트 확인 | `[ ]` |
| O-3 | 검색어 지우면 경고 사라짐 | 검색어 제거 → 경고 없어짐 확인 | `[ ]` |
| O-4 | 검색 결과에 있는 지원자 패널에는 경고 미표시 | 검색 결과에 포함된 패널 → 경고 없음 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 경고 존재 확인
> document.querySelector('.panel-filter-warn')?.textContent
> // → "현재 검색 결과에 없는 지원자입니다"
> ```
>
> **수동 절차:**
> 1. 지원자 A 패널 열기
> 2. 검색창에 지원자 A가 없는 검색어 입력 (예: "zzz")
> 3. 패널 하단에 노란 경고 표시 확인
> 4. 검색어 지우기 → 경고 사라짐 확인

---

## P. 블라인드 심사 모드 (에이전트 A)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| P-1 | 설정 드로어에 "블라인드 심사 모드" 토글 표시 | ⚙ 열기 → 토글 존재 확인 | `[ ]` |
| P-2 | 블라인드 ON → 사이드바 이름이 "지원자 #N"으로 교체 | 토글 켜기 → `.applicant-name.blind-name` 텍스트 확인 | `[ ]` |
| P-3 | 블라인드 ON → 패널 탭 이름도 "지원자 #N" | 패널 열기 → `.panel-tab-name` 텍스트 확인 | `[ ]` |
| P-4 | 블라인드 ON → 패널 본문 h1도 "지원자 #N 포트폴리오" | `.md-h1` 텍스트 확인 | `[ ]` |
| P-5 | 블라인드 OFF → 실명 복귀 | 토글 끄기 → 원래 이름 표시 확인 | `[ ]` |
| P-6 | 블라인드 ON 상태에서 더블클릭 이름편집 비활성화 | 블라인드 이름 span 더블클릭 → 편집창 열리지 않음 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 사이드바 블라인드 이름 확인
> document.querySelector('.blind-name')?.textContent  // "지원자 #1"
> // 패널 탭 이름
> document.querySelector('.panel-tab-name')?.textContent  // "지원자 #1 N%"
> ```

---

## Q. TopBar 버튼 아이콘화 (에이전트 B)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Q-1 | 내보내기 버튼이 `⬇` 아이콘만 표시 (텍스트 라벨 없음) | TopBar 확인 → "내보내기" 텍스트 없음, `⬇` 아이콘 버튼 확인 | `[ ]` |
| Q-2 | 불러오기 버튼이 `⬆` 아이콘만 표시 | 동일 | `[ ]` |
| Q-3 | `⬇` 호버 시 "내보내기 (JSON)" 툴팁 표시 | 마우스 호버 → 브라우저 네이티브 title 확인 | `[ ]` |
| Q-4 | `⬇` 클릭 → JSON 파일 다운로드 (로직 유지) | 클릭 → 파일 다운로드 확인 | `[ ]` |
| Q-5 | `⬆` 클릭 → 파일 선택 다이얼로그 열림 | 클릭 → input[file] 트리거 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 아이콘 버튼 존재 확인
> document.querySelectorAll('.topbar-icon-btn').length  // ≥ 2
> // title 속성
> document.querySelectorAll('.topbar-icon-btn')[0]?.title  // "내보내기 (JSON)"
> ```

---

## R. 패널 전체화면 모드 (에이전트 C)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| R-1 | 패널 탭 액션에 `⊞` 전체화면 버튼 표시 | `.panel-action-btn` 중 `⊞` 텍스트 버튼 존재 확인 | `[ ]` |
| R-2 | `⊞` 클릭 → 패널이 뷰포트 전체 덮음 + `⊡` 아이콘으로 변경 | 클릭 후 `.panel-fullscreen` 클래스 추가 확인 | `[ ]` |
| R-3 | `⊡` 클릭 → 패널 원래 크기로 복귀 | 재클릭 → `.panel-fullscreen` 클래스 제거 확인 | `[ ]` |
| R-4 | 전체화면 중 `Esc` 키 → 전체화면 해제 | 전체화면 상태에서 Esc → 패널 복귀 확인 | `[ ]` |
| R-5 | 다른 패널은 영향받지 않음 | 패널 A 전체화면 진입 → 패널 B 클래스 변화 없음 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 전체화면 클래스 확인
> document.querySelector('.panel-fullscreen') !== null  // true면 활성
> // 전체화면 버튼 텍스트
> document.querySelectorAll('.panel-action-btn')[0]?.textContent  // '⊞' 또는 '⊡'
> ```
>
> **수동 절차:**
> 1. 패널 열기
> 2. 탭 우측 `⊞` 클릭 → 전체화면 진입 확인
> 3. `⊡` 재클릭 → 복귀 확인
> 4. 다시 전체화면 → Esc → 복귀 확인

---

## Z. 기존 기능 회귀 테스트

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z-1 | 전체 포트폴리오 목록 정상 로드 | 페이지 로드 → 사이드바에 포트폴리오 목록 표시 | `[x]` (11명 표시 확인) |
| Z-2 | 지원자 클릭 → 우측 패널 열림 | 항목 클릭 → 패널 표시 확인 | `[x]` (스크린샷 확인됨) |
| Z-3 | 분석 버튼 → match_score 업데이트 | "분석" 클릭 → 뱃지 % 변경 확인 | `[ ]` |
| Z-4 | 지원자 검색 → 사이드바 필터링 | 검색어 입력 → 결과 필터 확인 | `[ ]` |
| Z-5 | 패널 내 intra 검색 → 하이라이트 | intra 검색창 입력 → 노란 하이라이트 확인 | `[ ]` |
| Z-6 | 원본 보기 버튼이 패널 상단에 표시 | `.panel-top-actions` 내 버튼 확인 | `[x]` (스크린샷 확인됨) |
| Z-7 | export 기능 정상 동작 | "내보내기" 클릭 → JSON 파일 다운로드 | `[ ]` |
| Z-8 | 설정 드로어 토글 정상 동작 | ⚙ 클릭 → 드로어 열림/닫힘 확인 | `[ ]` |
| Z-9 | localStorage 캐시 — 새로고침 후 포트폴리오 유지 | F5 새로고침 후 목록 동일한지 확인 | `[ ]` |
| Z-10 | 동기화 스크롤 ON 시 패널 동시 스크롤 | 2개 패널, 동기화 켜고 스크롤 확인 | `[ ]` |

---

## 검증 실행 방법

```bash
# 서버 실행 후
start.bat

# Playwright 브라우저 열기
# Claude Code에서: mcp__playwright__browser_navigate url=http://localhost:5173
```

### G-3 Esc 버그 재검증 절차

```
1. http://localhost:5173 접속
2. 첫 번째 지원자 클릭 (패널 열기)
3. browser_evaluate: document.body.click()   ← body 포커스 강제
4. browser_press_key: Escape
5. browser_evaluate: document.querySelectorAll('.portfolio-panel').length
   → 0이면 정상, 1이면 버그 지속
```

---

## S. 삭제 Undo 토스트 (에이전트 D)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| S-1 | 삭제 후 "취소" 버튼 포함 토스트 5초 표시 | 지원자 삭제 → `.toast-undo-btn` 존재 확인 | `[ ]` |
| S-2 | "취소" 클릭 → 목록에 지원자 복구 | 취소 클릭 → 사이드바에 이름 재표시 확인 | `[ ]` |
| S-3 | 복구 후 "삭제 취소됨" info 토스트 표시 | `.toast-info` 클래스 toast 등장 확인 | `[ ]` |
| S-4 | 5초 경과 후 취소 버튼 사라짐 (복구 불가) | 5초 대기 → toast 소멸, 목록 삭제 상태 유지 | `[ ]` |
| S-5 | 일반 toast에는 취소 버튼 미표시 | 추가·재분석 toast 확인 → 취소 버튼 없음 | `[ ]` |

> **검증 방법:**
> ```js
> // 취소 버튼 존재 확인
> document.querySelector('.toast-undo-btn')?.textContent  // "취소"
> ```

---

## T. 패널 스크롤 위치 복원 (에이전트 E)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| T-1 | 스크롤 후 패널 닫기 → 재오픈 시 이전 위치 복원 | 아래로 스크롤 → ✕ 닫기 → 재클릭 → scrollTop 유지 확인 | `[ ]` |
| T-2 | 처음 여는 패널은 최상단에서 시작 | 첫 오픈 패널 scrollTop === 0 확인 | `[ ]` |
| T-3 | 동기화 스크롤과 충돌 없음 | sync ON 상태에서 스크롤 → 다른 패널도 정상 이동 | `[ ]` |
| T-4 | 스크롤 debounce 200ms — 너무 잦은 저장 없음 | 빠른 스크롤 후 App scrollPos 상태 확인 (한 번만 갱신) | `[ ]` |

> **수동 절차:**
> 1. 패널 열기 → 하단으로 스크롤
> 2. 패널 닫기(✕)
> 3. 사이드바에서 동일 지원자 재클릭
> 4. 패널이 이전 스크롤 위치로 열리는지 확인

---

## U. 다크 모드 (에이전트 F)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| U-1 | 설정 드로어에 "다크 모드" 토글 표시 | ⚙ 열기 → 토글 확인 | `[ ]` |
| U-2 | 토글 ON → `body.dark` 클래스 추가 | DevTools `<body class="">` 확인 | `[ ]` |
| U-3 | 배경 `#0F172A`, 텍스트 `#F1F5F9`로 전환 | 배경색 시각 확인 | `[ ]` |
| U-4 | 입력창·textarea도 다크 처리 | 검색창, 스펙 입력, 메모 영역 색상 확인 | `[ ]` |
| U-5 | 토글 OFF → 라이트 모드 즉시 복귀 | 토글 끄기 → 원래 색상 복귀 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // dark 클래스 확인
> document.body.classList.contains('dark')  // true/false
> ```

---

## V. 검색 히스토리 드롭다운 (에이전트 G)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| V-1 | 검색어 2글자 이상 입력 후 localStorage에 `search-history` 저장 | 검색 → DevTools Application → localStorage 확인 | `[ ]` |
| V-2 | 검색창 focus 시 저장된 히스토리 드롭다운 표시 | localStorage에 히스토리 있는 상태에서 클릭 → 드롭다운 등장 | `[ ]` |
| V-3 | 히스토리 항목 클릭 → 해당 검색어로 즉시 검색 | 항목 클릭 → 사이드바 필터링 확인 | `[ ]` |
| V-4 | "히스토리 지우기" 클릭 → localStorage 삭제 + 드롭다운 사라짐 | 지우기 클릭 → `localStorage.getItem('search-history')` → null | `[ ]` |
| V-5 | 중복 검색어 저장 안 됨 (최대 8개) | 동일 검색어 반복 → 히스토리 목록 중복 없음 확인 | `[ ]` |

> **검증 방법:**
> ```js
> // 히스토리 확인
> JSON.parse(localStorage.getItem('search-history'))  // ["검색어1", "검색어2", ...]
> // 드롭다운 존재
> document.querySelector('.search-history-dropdown') !== null
> ```

---

## W. 즐겨찾기 북마크 (에이전트 H)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| W-1 | ☆ 클릭 → ★ 변경 + 항목 사이드바 최상단 이동 | 지원자 ☆ 클릭 → 상단 고정 확인 | `[ ]` |
| W-2 | ★ 재클릭 → ☆ 복귀 + 원래 순서로 이동 | 북마크 해제 → 위치 복귀 확인 | `[ ]` |
| W-3 | 북마크·일반 그룹 사이 `.bookmark-divider` 구분선 표시 | 북마크 1개 이상 시 구분선 존재 확인 | `[ ]` |
| W-4 | localStorage `portfolio-reviewer-bookmarks` 저장 + 새로고침 유지 | 북마크 후 F5 → 위치 유지 확인 | `[ ]` |
| W-5 | ★/☆ 클릭이 패널 토글에 영향 없음 (`stopPropagation`) | ☆ 클릭 → 패널 열리지 않음 확인 | `[ ]` |

> ```js
> JSON.parse(localStorage.getItem('portfolio-reviewer-bookmarks'))  // [id1, id2, ...]
> document.querySelectorAll('.bookmark-btn.bookmarked').length  // 북마크 수
> ```

---

## X. 패널 검색 이전·다음 이동 (에이전트 I)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| X-1 | 여러 결과 검색 시 `1/N` 형식 카운터 + ▲▼ 버튼 표시 | 반복 단어 검색 → 카운터 + 버튼 확인 | `[ ]` |
| X-2 | ▼ 클릭 → 다음 결과로 스크롤 + 주황 아웃라인 포커스 | ▼ 클릭 → `intra-focused` 클래스 + 스크롤 확인 | `[ ]` |
| X-3 | ▲▼ 순환 동작 (마지막→처음, 처음→마지막) | N/N에서 ▼ → 1/N 복귀 확인 | `[ ]` |
| X-4 | 검색어 변경 시 카운터 1로 리셋 | 새 검색어 입력 → `1/N` 리셋 확인 | `[ ]` |
| X-5 | 결과 1건 시 버튼 disabled | 1회 등장 단어 검색 → ▲▼ opacity 0.35 확인 | `[ ]` |

> ```js
> document.querySelectorAll('.intra-nav-btn').length  // 2 (▲▼)
> document.querySelector('.intra-highlight.intra-focused')  // 현재 포커스 요소
> ```

---

## Y. 업로드 단계별 진행 표시 (에이전트 K)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Y-1 | 업로드 시 단계 표시: 📄→🤖→💾→✅ 순서 | 파일 업로드 → 모달 내 단계 진행 확인 | `[ ]` |
| Y-2 | 완료된 단계는 ✓ 초록 표시 | 각 단계 완료 후 이전 단계 아이콘 변경 확인 | `[ ]` |
| Y-3 | 완료(✅) 후 800ms → 모달 자동 닫힘 | 완료 후 자동 닫힘 타이밍 확인 | `[ ]` |
| Y-4 | 에러 시 단계 표시 초기화 | 잘못된 파일 → `upload-steps` 사라짐 확인 | `[ ]` |

> **수동 절차:**
> 1. "+ 추가" 클릭 → 파일 선택
> 2. 모달에 📄 "파일 읽는 중..." 표시 확인
> 3. 🤖 "LLM 파싱 중..." → 💾 "저장 중..." → ✅ "완료!" 순서 확인
> 4. 완료 후 약 1초 내 모달 자동 닫힘 확인

---

## Z2. 점수 가중치 커스텀 슬라이더 (에이전트 J)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z2-1 | 설정 드로어 하단에 3개 슬라이더 표시 (스킬/경력/프로젝트) | ⚙ 열기 → 슬라이더 섹션 확인 | `[ ]` |
| Z2-2 | 슬라이더 조작 시 나머지 두 항목 자동 조정, 합계 100% 유지 | 스킬 80% 설정 → 합계 100% 확인 | `[ ]` |
| Z2-3 | 가중치 변경 즉시 사이드바 순위 재정렬 | 프로젝트 비율 올리기 → 프로젝트 많은 지원자 상위 이동 | `[ ]` |
| Z2-4 | Network에서 `/api/analyze` POST body에 `weights` 필드 확인 | DevTools → Network → analyze 요청 payload 확인 | `[ ]` |
| Z2-5 | 기본값: 스킬 60 / 경력 25 / 프로젝트 15 | 페이지 새로고침 → 드로어 슬라이더 기본값 확인 | `[ ]` |

> ```js
> // Network 탭 대신 콘솔에서 확인
> // 분석 후 사이드바 점수 변화 확인
> document.querySelectorAll('.match-badge')[0]?.textContent  // "N%"
> ```

---

## Z3. 채용 공고 스펙 자동 추출 (에이전트 L)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z3-1 | TopBar 스펙 입력창 옆 📋 버튼 표시 | 상단 바 확인 → 📋 버튼 존재 | `[ ]` |
| Z3-2 | 📋 클릭 → 채용 공고 textarea 팝업 표시 | 클릭 → `.job-desc-popup` 등장 확인 | `[ ]` |
| Z3-3 | 텍스트 붙여넣고 "스펙 자동 추출" → 스펙 입력창 자동 채워짐 | Solar API 있는 환경에서 실제 추출 확인 | `[ ]` |
| Z3-4 | Solar API 없을 때 에러 alert 표시 | `.env` 없는 환경에서 추출 → alert 확인 | `[ ]` |
| Z3-5 | `/api/extract-specs` 엔드포인트 FastAPI docs 등록 | `http://localhost:8000/docs` → utils 태그 확인 | `[ ]` |

> **수동 절차:**
> 1. 상단 바 스펙 입력창 오른쪽 📋 버튼 클릭
> 2. 팝업 textarea에 채용 공고 텍스트 붙여넣기
> 3. "🔍 스펙 자동 추출" 클릭
> 4. 스펙 입력창에 키워드 자동 입력 확인

---

## Z4. 사이드바 조건 필터 (에이전트 M)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z4-1 | 사이드바 헤더에 ⚙ 필터 버튼 표시 | 헤더 우측 ⚙ 버튼 존재 확인 | `[ ]` |
| Z4-2 | 경력 N년 이상 필터 동작 | 경력 최소 `3` 입력 → career_years < 3 지원자 제외 확인 | `[ ]` |
| Z4-3 | 스킬 포함 필터 동작 | `React` 입력 → React 미보유 지원자 제외 확인 | `[ ]` |
| Z4-4 | 필터 활성 시 헤더 카운트 `N명 / 전체M명` 표시 | 필터 적용 후 헤더 텍스트 확인 | `[ ]` |
| Z4-5 | "필터 초기화" 클릭 → 전체 목록 복귀 | 초기화 버튼 클릭 → 전체 지원자 표시 확인 | `[ ]` |

> ```js
> document.querySelector('.sidebar-filter-panel')  // 필터 패널 존재 확인
> document.querySelector('.sidebar-header')?.textContent  // "N명 / 전체M명"
> ```

---

## Z5. 툴팁·패널 카운트 뱃지 (에이전트 N)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z5-1 | 동기화 스크롤 토글에 상태별 툴팁 표시 | 토글 hover → title 텍스트 확인 | `[ ]` |
| Z5-2 | 패널 1개 이상 열리면 `패널 N/4` 뱃지 표시 | 지원자 클릭 → `.panel-count-badge` 등장 확인 | `[ ]` |
| Z5-3 | 패널 0개 → 뱃지 미표시 | 패널 모두 닫기 → 뱃지 없음 확인 | `[ ]` |
| Z5-4 | 빈 상태 메시지 "최대 4개" 문구 포함 | 포트폴리오 있고 패널 없는 상태 확인 | `[ ]` |

---

## Z6. BST 인덱스 캐싱 (에이전트 P)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z6-1 | 변경 없이 동일 검색 2회 → 2번째가 빠름 | 검색 응답 시간 비교 (DevTools Network) | `[ ]` |
| Z6-2 | 포트폴리오 추가 후 새 지원자 검색에서 검색됨 | 추가 후 즉시 검색 → 새 지원자 포함 확인 | `[ ]` |
| Z6-3 | 삭제된 지원자가 검색 결과에서 제거됨 | 삭제 후 검색 → stale 결과 없음 확인 | `[ ]` |

> **구현 범위:** 완전 구현 — 추가/삭제/재분석/임포트 4개 지점 모두 `invalidate_cache()` 연결

---

---

## Z7. 스킬 매트릭스 (에이전트 O)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z7-1 | TopBar에 ⊞ 스킬 매트릭스 버튼 표시 | `.topbar-icon-btn` 중 ⊞ 텍스트 버튼 확인 | `[ ]` |
| Z7-2 | ⊞ 클릭 → `.matrix-overlay` 모달 등장 | 클릭 후 `document.querySelector('.matrix-overlay')` 존재 확인 | `[ ]` |
| Z7-3 | 모달에 지원자 × 스킬 격자 테이블 표시 | `.matrix-table` 존재 + 행(지원자) × 열(스킬) 구조 확인 | `[ ]` |
| Z7-4 | 보유 스킬 셀에 `has-skill` 클래스 + ✓ 표시 | `.has-skill` 셀 개수 > 0 확인 | `[ ]` |
| Z7-5 | 미보유 셀은 `no-skill` 클래스 | `.no-skill` 셀 개수 확인 | `[ ]` |
| Z7-6 | 스킬 열이 빈도순 정렬 (가장 많이 보유한 스킬이 왼쪽) | 첫 번째 열 스킬이 가장 많은 지원자가 보유한 스킬인지 확인 | `[ ]` |
| Z7-7 | 오버레이 클릭 또는 ✕ 버튼 → 모달 닫힘 | 오버레이 클릭 → `.matrix-overlay` 사라짐 확인 | `[ ]` |

> ```js
> document.querySelectorAll('.matrix-table tr').length  // 지원자 수 + 헤더 행
> document.querySelectorAll('.has-skill').length  // 총 보유 스킬 수
> ```

---

## Z8. 파싱 결과 인라인 편집 (에이전트 Q)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z8-1 | 경력 연도 클릭 → 숫자 입력창으로 전환 | 경력 값 클릭 → `input[type=number]` 등장 확인 | `[ ]` |
| Z8-2 | 학력 클릭 → 텍스트 입력창으로 전환 | 학력 값 클릭 → `input[type=text]` 등장 확인 | `[ ]` |
| Z8-3 | 자기소개 클릭 → textarea로 전환 | 자기소개 영역 클릭 → `textarea.inline-edit-textarea` 등장 확인 | `[ ]` |
| Z8-4 | Enter 또는 blur → 값 저장, 표시 업데이트 | 경력 수정 후 Enter → 새 값 표시 확인 | `[ ]` |
| Z8-5 | Escape → 편집 취소 (원래 값 복귀) | Escape 키 → 입력창 사라짐 + 원래 값 유지 확인 | `[ ]` |
| Z8-6 | 수정된 경우 탭에 ✏ 뱃지(`.panel-edited-badge`) 표시 | 편집 후 `.panel-edited-badge` 존재 확인 | `[ ]` |
| Z8-7 | localStorage `portfolio-reviewer-overrides` 저장 | 수정 후 `localStorage.getItem('portfolio-reviewer-overrides')` 값 확인 | `[ ]` |
| Z8-8 | 새로고침 후 편집값 복원 | F5 → 동일 패널 열기 → 수정된 값 유지 확인 | `[ ]` |

> ```js
> const ov = JSON.parse(localStorage.getItem('portfolio-reviewer-overrides'));
> console.log(ov);  // { [id]: { career_years: 3, education: "...", intro: "..." } }
> ```

---

## Z9. 2인 diff 비교 API (에이전트 R)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z9-1 | `/api/diff` 엔드포인트가 FastAPI docs에 등록됨 | `http://localhost:8000/docs` → POST /api/diff 항목 확인 | `[ ]` |
| Z9-2 | Solar 없이 로컬 diff 반환 (fallback) | Solar API 없는 환경에서 POST → `solar: false` 응답 확인 | `[ ]` |
| Z9-3 | 응답에 경력/학력/기술스택 비교 정보 포함 | `diff.경력.A`, `diff.경력.B`, `diff.경력.winner` 필드 존재 확인 | `[ ]` |
| Z9-4 | 존재하지 않는 ID → 404 오류 | `{ id_a: "xxx", id_b: "yyy" }` 요청 → 404 응답 확인 | `[ ]` |
| Z9-5 | `api.js` `diffPortfolios(idA, idB)` 함수 존재 | 함수 import 후 호출 가능 확인 | `[ ]` |

> **수동 절차:**
> ```bash
> curl -X POST http://localhost:8000/api/diff \
>   -H "Content-Type: application/json" \
>   -d '{"id_a": "ID1", "id_b": "ID2"}'
> # → { "diff": {...}, "name_a": "...", "name_b": "...", "solar": false }
> ```

---

## Z10. 히스토리 추가 일시 (에이전트 S)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z10-1 | 신규 업로드 포트폴리오에 `_added_at` 필드 저장 | 업로드 후 session.json 에서 `_added_at` 키 확인 | `[ ]` |
| Z10-2 | 날짜 형식 `YYYY-MM-DD HH:MM` | `_added_at` 값 형식 확인 (예: "2026-05-20 14:30") | `[ ]` |
| Z10-3 | 패널 기본 정보 섹션에 "추가됨 YYYY-MM-DD HH:MM" 표시 | 업로드 포트폴리오 패널 열기 → 기본 정보에 추가됨 항목 확인 | `[ ]` |
| Z10-4 | 기존 포트폴리오(`_added_at` 없음) 패널에는 추가됨 항목 미표시 | 이전 업로드 포트폴리오 → 추가됨 행 없음 확인 | `[ ]` |

> ```js
> [...document.querySelectorAll('.md-bold')].map(e => e.textContent)
> // → ["경력", "학력", "이메일", "GitHub", "추가됨"] (추가됨 있으면)
> ```

---

## Z11. 지원자별 메모·코멘트 (이전 구현 확인)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z11-1 | 패널 하단에 메모 textarea 표시 | `.panel-note-textarea` 존재 확인 | `[ ]` |
| Z11-2 | 메모 입력 → localStorage `portfolio-reviewer-notes` 저장 | 입력 후 400ms → localStorage 확인 | `[ ]` |
| Z11-3 | 새로고침 후 메모 유지 | F5 → 패널 열기 → 메모 복원 확인 | `[ ]` |

---

---

## Z12. 2인 diff 비교 프론트엔드 (에이전트 T)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z12-1 | 패널 2개 열렸을 때만 툴바에 "⇄ 비교" 버튼 표시 | 패널 1개 → 버튼 없음, 패널 2개 → 버튼 있음 확인 | `[ ]` |
| Z12-2 | 패널 3~4개일 때 버튼 미표시 | 패널 3개 열기 → 버튼 없음 확인 | `[ ]` |
| Z12-3 | "⇄ 비교" 클릭 → `.diff-overlay` 모달 등장 | 클릭 → `document.querySelector('.diff-overlay')` 존재 확인 | `[ ]` |
| Z12-4 | 모달 헤더에 "A이름 vs B이름" 표시 | 모달 h2 텍스트 확인 | `[ ]` |
| Z12-5 | 로딩 중 "비교 분석 중..." 표시 | 모달 열리자마자 로딩 텍스트 확인 | `[ ]` |
| Z12-6 | diff 결과 테이블에 항목/A열/B열 표시 | `.diff-table` 내 행 개수 > 0 확인 | `[ ]` |
| Z12-7 | winner 항목 셀에 녹색 배경 + ✓ 아이콘 | winner가 있는 행의 해당 셀 배경 확인 | `[ ]` |
| Z12-8 | Solar API 없을 때 "로컬 비교" 배지, 있을 때 "Solar AI 분석" 배지 | 배지 텍스트 확인 | `[ ]` |
| Z12-9 | ✕ 클릭 → 모달 닫힘 | 닫기 후 `.diff-overlay` 없음 확인 | `[ ]` |
| Z12-10 | Esc 키 → 모달 닫힘 | Esc → `.diff-overlay` 사라짐 확인 | `[ ]` |
| Z12-11 | 오버레이 배경 클릭 → 닫힘 | 모달 외부 클릭 → 닫힘 확인 | `[ ]` |

> ```js
> document.querySelector('.diff-overlay')  // null이면 닫힘
> document.querySelectorAll('.diff-table tr').length  // 항목 수 + 헤더 = N+1
> ```
>
> **수동 절차:**
> 1. 지원자 2명 패널 열기
> 2. 툴바 "⇄ 비교" 버튼 클릭
> 3. 모달 내 A vs B 비교 테이블 확인
> 4. winner 열 녹색 강조 확인
> 5. Esc 또는 ✕로 닫기 확인

---

---

## Z13. 사이드바 이름 빠른 검색 + 날짜 필터 (에이전트 U)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z13-1 | 사이드바 헤더 아래에 `.sidebar-name-filter` 입력창 표시 | `document.querySelector('.sidebar-name-filter')` 존재 확인 | `[ ]` |
| Z13-2 | 이름 입력 → 해당 이름 포함 지원자만 표시 | "김" 입력 → 김씨 성 지원자만 남음 확인 | `[ ]` |
| Z13-3 | 이름 필터에 값 있을 때 ✕ 클리어 버튼 표시 | 입력 후 `.sidebar-name-filter-clear` 존재 확인 | `[ ]` |
| Z13-4 | ✕ 클릭 → 필터 초기화, 전체 목록 복귀 | 버튼 클릭 → 전체 지원자 표시 확인 | `[ ]` |
| Z13-5 | ⚙ 필터 패널에 날짜 범위 필터 추가됨 | ⚙ 열기 → `.filter-date-row` 존재 확인 | `[ ]` |
| Z13-6 | `_added_at` 있는 지원자에 날짜 필터 적용 | 날짜 범위 설정 → 해당 날짜 외 지원자 제외 확인 | `[ ]` |
| Z13-7 | `_added_at` 없는 지원자는 날짜 필터 시 제외됨 | 날짜 필터 적용 → 구 지원자 숨김 확인 | `[ ]` |
| Z13-8 | 필터 초기화 버튼이 날짜도 리셋 | 초기화 클릭 → 날짜 입력 비워짐 + 전체 목록 복귀 | `[ ]` |

> ```js
> document.querySelector('.sidebar-name-filter')?.value   // 현재 입력값
> document.querySelector('.filter-date-row')              // 날짜 필터 행 존재 확인
> ```

---

## Z14. Solar API rate limit 재시도 backoff (에이전트 V)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z14-1 | `solar.py`에 `_call_solar_api()` 헬퍼 함수 추가됨 | 파일 내 함수 존재 확인 | `[ ]` |
| Z14-2 | 429 응답 시 지수 백오프 후 재시도 | 백엔드 로그 `[Solar] rate limit, retry 1/3 after 1.0s` 확인 | `[ ]` |
| Z14-3 | 3회 실패 시 `SolarAPIError` 발생 | rate limit 환경 시뮬레이션 → 적절한 에러 반환 확인 | `[ ]` |
| Z14-4 | 503 응답 시 1회 재시도 후 실패 | 503 환경에서 1회만 재시도 로그 확인 | `[ ]` |
| Z14-5 | 기존 `parse_text()` 인터페이스 유지 | 정상 API 키로 파싱 요청 → 기존과 동일 결과 반환 확인 | `[ ]` |

> **구현 범위:** 동기 `requests` + `time.sleep` 기반. `NotConfiguredError` 인터페이스 유지.

---

---

## Z8 (개정). 파싱 결과 인라인 편집 — 제거됨

> 사용자 정책 "지원자의 포트폴리오는 수정되어서는 안됩니다"에 따라 인라인 편집 기능 전체 제거.
> `OVERRIDES_KEY`, `loadOverrides`, `overrides`, `saveOverride`, `editField` 상태 및 관련 JSX 모두 삭제.
> `panel-edited-badge` 뱃지 미표시.
> **Z8 검증 항목은 더 이상 적용되지 않음 — SKIP.**

---

## Z15. 중요 링크 버튼 바 (이번 세션)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z15-1 | `solar.py` 스키마에 `links` 배열 필드 추가됨 | `_SCHEMA` 내 `"links": [{"label":"...","url":"..."}]` 확인 | `[ ]` |
| Z15-2 | 신규 업로드 포트폴리오에 `links` 필드 파싱됨 | 업로드 후 session.json `links` 키 확인 | `[ ]` |
| Z15-3 | 패널 본문 상단 `h1` 아래에 `.panel-links-bar` 표시 | 링크 보유 포트폴리오 패널 열기 → 링크 버튼 줄 존재 확인 | `[ ]` |
| Z15-4 | GitHub URL → 🐙 아이콘 + "GitHub" 텍스트 버튼 | github.com URL 버튼 아이콘 확인 | `[ ]` |
| Z15-5 | Notion URL → 📝 아이콘 | notion.so URL 버튼 아이콘 확인 | `[ ]` |
| Z15-6 | `a.github` 필드가 있으면 links에 없어도 버튼 표시 | github 필드만 있는 포트폴리오 → 버튼 표시 확인 | `[ ]` |
| Z15-7 | 링크 버튼 클릭 → 새 탭에서 URL 열림 | 버튼 `target="_blank"` + `rel="noopener"` 확인 | `[ ]` |
| Z15-8 | 링크 없는 포트폴리오에는 링크 바 미표시 | links=[], github="" 포트폴리오 → `.panel-links-bar` 없음 확인 | `[ ]` |

---

## Z16. 마킹 드롭다운 배경 불투명 수정 (이번 세션)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z16-1 | 마킹 버튼(·) 클릭 시 드롭다운 메뉴 표시됨 | `.mark-btn` 클릭 → `.mark-dropdown` 등장 확인 | `[ ]` |
| Z16-2 | 드롭다운 배경이 흰색(#ffffff) — 투명하지 않음 | `.mark-dropdown` computed background-color 확인 | `[ ]` |
| Z16-3 | z-index 1000으로 다른 요소 위에 표시됨 | 드롭다운이 메모·패널 내용 위에 표시되는지 확인 | `[ ]` |
| Z16-4 | 다크 모드에서 드롭다운 배경 #1E293B | body.dark 상태에서 드롭다운 배경색 확인 | `[ ]` |
| Z16-5 | 통과/보류/탈락/마킹없음 4개 항목 선택 가능 | 각 항목 클릭 → 마킹 상태 변경 확인 | `[ ]` |
| Z16-6 | 드롭다운 외부 클릭 시 닫힘 | 사이드바 다른 곳 클릭 → 드롭다운 사라짐 확인 | `[ ]` |

---

## Z17. 삭제 Undo 토스트 클릭 가능 수정 (이번 세션)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z17-1 | 삭제 후 "취소" 버튼 포함 토스트 표시됨 | 삭제 → `.toast-undo-btn` 존재 확인 | `[ ]` |
| Z17-2 | "취소" 버튼이 클릭 가능함 (pointer-events: auto) | `.toast` CSS `pointer-events: auto` 확인 | `[ ]` |
| Z17-3 | "취소" 클릭 → 지원자 복구됨 | 삭제 → 취소 클릭 → 사이드바 복구 확인 | `[ ]` |
| Z17-4 | 포트폴리오 메모 textarea가 토스트 클릭을 막지 않음 | 메모 입력창이 보이는 상태에서 toast 취소 버튼 클릭 가능 확인 | `[ ]` |

---

## Z18. 채용 공고 Solar LLM 추출 개선 (이번 세션)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z18-1 | 비정형 자연어 공고 텍스트 추출 성공 | "게임개발부 프론트엔드 사이트 제작 담당자 모집. 경력 1년 이상 필수사항: AWS, JS , 우대사항: SQL, C#, Unity 관련" 붙여넣기 → 스펙 추출 확인 | `[ ]` |
| Z18-2 | 응답에 `required`·`preferred` 배열 분리됨 | API 응답 JSON에 두 필드 존재 확인 | `[ ]` |
| Z18-3 | 스펙 입력창에 추출된 키워드 자동 채워짐 | 추출 후 TopBar 스펙 입력창 값 확인 | `[ ]` |
| Z18-4 | Solar 응답이 JSON이 아닌 경우 폴백(쉼표 분리) 동작 | 비정상 응답 시 에러 없이 키워드 리스트 반환 | `[ ]` |

> **수동 절차:**
> 1. 📋 버튼 클릭 → 팝업 textarea에 "게임개발부 프론트엔드 사이트 제작 담당자 모집. 경력 1년 이상 필수사항: AWS, JS , 우대사항: SQL, C#, Unity 관련" 붙여넣기
> 2. "🔍 스펙 자동 추출" 클릭
> 3. 스펙 입력창에 "AWS, JS, SQL, C#, Unity" 등 자동 입력 확인

---

---

## Z19. 스켈레톤 UI 분석 로딩 (에이전트 aa3be5)

| # | 검증 항목 | 방법 | 결과 |
|---|-----------|------|------|
| Z19-1 | 분석 버튼 클릭 시 사이드바 목록 자리에 shimmer 스켈레톤 3개 표시 | 분석 클릭 → `.skeleton-card` 3개 등장 확인 | `[ ]` |
| Z19-2 | 분석 완료 후 스켈레톤 → 실제 목록으로 교체 | 분석 완료 후 `.applicant-item` 목록 복귀 확인 | `[ ]` |
| Z19-3 | `App.jsx` `analyzing` state가 runAnalyze 전후로 true/false 전환 | Network 탭에서 analyze 요청 동안 스켈레톤 유지 확인 | `[ ]` |
| Z19-4 | 스켈레톤 중 필터·북마크 등 기존 기능 영향 없음 | 분석 완료 후 필터 동작 확인 | `[ ]` |

> **구현 범위:** `App.jsx` `analyzing` state + `runAnalyze` 래핑, `Sidebar.jsx` `analyzing` prop 조건부 skeleton 렌더링

---

*최종 업데이트: 2026-05-20 (Z15~Z19 섹션 추가, Z8 개정)*
